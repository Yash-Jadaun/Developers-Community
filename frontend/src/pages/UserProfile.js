// src/pages/UserProfile.js
import React from 'react';

const UserProfile = () => {
    return (
        <div>
            <h1>User Profile</h1>
            <p>This is the User Profile page, used for both Sign Up and Sign In.</p>
        </div>
    );
};

export default UserProfile;
